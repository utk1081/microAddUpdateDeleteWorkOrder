apiVersion: v1
kind: Service
metadata:
  name: service-add-update-delete
spec:
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30008
      protocol: TCP
  type: NodePort
  selector:
    name: label-add-update-delete
