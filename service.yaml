apiVersion: v1
kind: Service
metadata:
  name: updateservice
spec:
  ports:
    - port: 8084
      targetPort: 8080
      #nodePort: 30003
      protocol: TCP
  type: LoadBalancer
  selector:
    name: update-label
